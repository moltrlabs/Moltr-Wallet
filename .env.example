# Moltr API — copy to .env and fill. Never commit .env or real keys.

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Database — SQLite for local dev; Postgres for production
# SQLite (dev):
DATABASE_URL="file:./dev.db"
# Postgres (prod): change prisma/schema.prisma provider to "postgresql" and use:
# DATABASE_URL="postgresql://user:password@host:5432/moltr?schema=public"

# CORS — comma-separated allowed origins
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Receipt URLs — base URL for receipt links (e.g. https://api.moltr.app/r)
RECEIPT_BASE_URL=https://api.moltr.app/r

# Object storage (S3-compatible: AWS S3 or Cloudflare R2)
# Leave unset for local dev if you skip object uploads or use a local S3 mock
S3_BUCKET=moltr-objects
S3_REGION=auto
S3_ENDPOINT=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_FORCE_PATH_STYLE=false
# Public URL for objects (e.g. https://cdn.moltr.app)
CDN_BASE_URL=https://cdn.moltr.app
